{
  "name": "@solarwinds-apm/examples",
  "private": true,
  "scripts": {
    "lint": "prettier --check . && eslint --max-warnings=0 *.js '*/**/*.{js,ts}'",
    "lint:fix": "eslint --fix *.js '*/**/*.{js,ts}' && prettier --write ."
  },
  "devDependencies": {
    "@solarwinds-apm/eslint-config": "workspace:^",
    "eslint": "^8.44.0",
    "prettier": "^3.0.0"
  }
}
