{
  "name": "@swotel/examples",
  "private": true,
  "scripts": {
    "lint": "prettier --check . && eslint --max-warnings=0 *.js '*/**/*.{js,ts}'",
    "lint:fix": "eslint --fix *.js '*/**/*.{js,ts}' && prettier --write ."
  },
  "devDependencies": {
    "@swotel/eslint-config": "workspace:^",
    "eslint": "^8.41.0",
    "prettier": "^2.8.8"
  }
}
