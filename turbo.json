{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "build.js",
        "package.json",
        "tsconfig.json",
        "tsconfig.build.json",
        "src/**",
        "oboe/**"
      ],
      "outputs": ["dist/**", "npm/*/oboe.node", "npm/*/liboboe.so"]
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint:fix": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "publish": {
      "dependsOn": ["^publish"],
      "outputs": []
    },
    "start": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["@solarwinds-apm/test#build", "build"],
      "inputs": ["package.json", "tsconfig.json", "src/**", "test/**"],
      "env": ["TEST_SERVICE_KEY", "TEST_COLLECTOR"],
      "outputs": []
    }
  },
  "globalDependencies": [
    ".clang-format",
    ".prettierrc.json",
    ".yarnrc.yml",
    "tsconfig.base.json",
    "yarn.lock",
    "packages/eslint-config/index.js"
  ]
}
