{
  "name": "@solarwinds-apm/scripts",
  "scripts": {
    "lint": "prettier --check . && eslint --max-warnings=0 *.js",
    "lint:fix": "eslint --fix *.js && prettier --write ."
  },
  "dependencies": {
    "axios": "^1.4.0",
    "bson": "^5.3.0"
  },
  "devDependencies": {
    "@solarwinds-apm/eslint-config": "workspace:^",
    "eslint": "^8.41.0",
    "prettier": "^2.8.8"
  }
}
