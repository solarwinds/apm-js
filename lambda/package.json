{
  "name": "@solarwinds-apm/lambda-shim",
  "private": true,
  "packageManager": "yarn@4.11.0",
  "dependencies": {
    "{{name}}": "{{version}}",
    "@opentelemetry/api": "{{api-version}}",
    "semver": "^7"
  }
}
