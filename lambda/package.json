{
  "name": "@solarwinds-apm/lambda-shim",
  "private": true,
  "packageManager": "yarn@4.1.1",
  "dependencies": {
    "{{name}}": "{{version}}",
    "@opentelemetry/api": "{{api-version}}",
    "semver": "^7"
  }
}
