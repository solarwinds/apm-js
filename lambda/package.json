{
	"name": "@solarwinds-apm/lambda-shim",
	"private": true,
	"dependencies": {
		"@opentelemetry/api": "^1.9.0",
		"semver": "^7",
		"{{name}}": "{{version}}"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^12.6.0"
		}
	}
}
