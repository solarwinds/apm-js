{
  "name": "@solarwinds-apm/lambda-shim",
  "private": true,
  "packageManager": "yarn@4.9.4",
  "dependencies": {
    "{{name}}": "{{version}}",
    "@opentelemetry/api": "{{api-version}}",
    "semver": "^7"
  }
}
